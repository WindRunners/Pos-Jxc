<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"/>
  <meta charset="utf-8"/>
  <title><%= content_for?(:title) ? yield(:title) : "Lark" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0"/>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "application", :media => "all"%>
  <%= javascript_include_tag "application"%>
  <%= javascript_include_tag "registjs/jquery.form" %>
  <style>
      span.error{height:12px; font-size:12px; font-family:"微软雅黑"; color:#C00;  line-height:12px; margin-left:2px; margin-top:8px;}
  </style>
</head>
<body style="width: 100%">
<div style="width: 100%;">

  <% if @error_msg.present? %>
      <div class="alert alert-danger" role="alert"><%= raw @error_msg %></div>
  <% end %>

  <% if @success_msg.present? %>
      <div class="alert alert-success" role="alert"><%= raw @success_msg %></div><br>
  <% else %>
      <%= form_tag(controller: "userinfo_orders",action: "binduserinfo",method: "post") %>
          <input type="hidden" name="check_mesage", value="true" />
          <input type="hidden" name="openid", value="<%= @openid %>" />

          <div class="form-group">
            <label for="mobile">管理员</label>
            <input type="text" class="form-control" id="mobile" value="<%= @mobile %>" name="mobile" placeholder="账号">
          </div>

          <div class="form-group">
            <label for="admin_pwd">密码</label>
            <input type="password" class="form-control col-sm-10" id="admin_pwd" name="password" placeholder="密码">
          </div>
          <button type="submit" class="btn btn-primary btn-lg btn-block">点击绑定</button>
  <% end %>

</div>
</body>
<script>

</script>
</html>