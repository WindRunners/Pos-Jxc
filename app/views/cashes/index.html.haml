可用金额：
= @pay_totle
&nbsp;&nbsp;
提现中金额：-
= @cashing_totle
&nbsp;&nbsp;
提现总额：
= @cash_totle
%br
= link_to "提现","","data-href"=>new_cash_path, :class=>"btn btn-primary"

%br
%h1 取现纪录

%table.table.table-striped.table-bordered.table-hover
  %thead
    %tr
      %th= "取现金额"
      %th= "取现状态"
      %th= "取现时间"
      /%th= "订单号"
      /%th= "订单状态"
      /%th= "订单类型"
      /%th= "订单金额"
      /%th= "订单时间"
      %th

  %tbody
  - @cashes.each do |cash|
    - if cash.userinfo_id==current_user.userinfo_id
      - if cash.cash_state==1
        %tr#cash_insert
          %td= "-#{cash.cash}"
          %td= cash.cash_state_to_s if cash.cash_state.present?
          %td= cash.data_show
          //%td= cash.orderno
          //%td= cash.pay_state
          //%td= cash.pay_type
          //%td= cash.paycost
          //%td= cash.pay_date.strftime('%Y-%m-%d %H:%M:%S').to_s if cash.pay_date.present?//
          %td= link_to 'Show', cash
      - if cash.cash_state==9
        %tr
          %td= "#{cash.cash}"
          %td= cash.cash_state_to_s if cash.cash_state.present?
          %td= cash.data_show
          //%td= cash.orderno
          //%td= cash.pay_state
          //%td= cash.pay_type
          //%td= cash.paycost
          //%td= cash.pay_date.strftime('%Y-%m-%d %H:%M:%S').to_s if cash.pay_date.present?//
          %td= link_to 'Show', cash
      - if cash.cash_state==2
        %tr
          %td= "-#{cash.cash}"
          %td= cash.cash_state_to_s if cash.cash_state.present?
          %td= cash.data_show
          //%td= cash.orderno
          //%td= cash.pay_state
          //%td= cash.pay_type
          //%td= cash.paycost
          //%td= cash.pay_date.strftime('%Y-%m-%d %H:%M:%S').to_s if cash.pay_date.present?//
          %td= link_to 'Show', cash
      - if cash.cash_state==4
        %tr
          %td= "-#{cash.cash}"
          %td= cash.cash_state_to_s if cash.cash_state.present?
          %td= cash.data_show
          //%td= cash.orderno
          //%td= cash.pay_state
          //%td= cash.pay_type
          //%td= cash.paycost
          //%td= cash.pay_date.strftime('%Y-%m-%d %H:%M:%S').to_s if cash.pay_date.present?//
          %td= link_to 'Show', cash


= paginate @cashes



%h1 完成订单纪录
%table.table.table-striped.table-bordered.table-hover
  %thead
    %tr
      %th= "订单号"
      %th= "订单状态"
      %th= "订单类型"
      %th= "订单金额"
      %th= "订单时间"

  %tbody
  - @cashOrders.each do |cashOrder|
    %tr
      %td= cashOrder.orderno if cashOrder.orderno
      %td= cashOrder.pay_state
      %td= cashOrder.pay_type
      %td= cashOrder.paycost
      %td= cashOrder.pay_date.strftime('%Y-%m-%d %H:%M:%S').to_s if cashOrder.pay_date.present?

= paginate @cashOrders
