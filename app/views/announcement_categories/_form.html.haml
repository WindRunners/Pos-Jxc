= form_for @announcement_category,id: 'announcementCategoryForm', remote:true do |f|
  - if @announcement_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@announcement_category.errors.count, "error")} prohibited this announcement_category from being saved:"
      %ul
        - @announcement_category.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
