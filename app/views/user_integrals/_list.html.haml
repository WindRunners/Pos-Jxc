%p
%table{:class => "table-center table table-striped table-bordered table-hover", :id => "integrals", "data-source" => "#{user_integrals_url(format: "json")}"}
  %thead
    %tr
      %th= "积分"
      %th= "状态"
      %th= "类型"
      %th= "操作日期"
  %tbody
:javascript
  $(document).ready(function () {
      userIntegralsIndex();
  });
